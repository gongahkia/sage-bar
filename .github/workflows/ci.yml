name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    strategy:
      matrix:
        os: [macos-14, macos-13]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_15.4.app
      - name: Install SwiftLint
        run: brew install swiftlint
      - name: Lint
        run: swiftlint --strict
      - name: Install swift-format
        run: brew install swift-format
      - name: swift-format lint
        run: |
          OUTPUT=$(swift-format lint --recursive Sources/ Tests/ 2>&1)
          [ -z "$OUTPUT" ] || { echo "$OUTPUT"; exit 1; }
      - name: Validate prices.json
        run: python3 -c "import json,sys; json.load(open('Sources/ClaudeUsage/Resources/prices.json'))"
      - name: Build app target (release)
        run: swift build --target ClaudeUsage -c release
      - name: Build CLI target (release)
        run: swift build --target ClaudeUsageCLI -c release
      - name: Build
        run: swift build
      - name: Test
        run: swift test --parallel
