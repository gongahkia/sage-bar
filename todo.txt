PROJECT: Claude Usage Tracker — macOS Menu Bar App + Fastfetch-style TUI HUD

DATA SOURCE PRIORITY: (1) ~/.claude/ local logs [no auth], (2) Anthropic workspace API [admin key], (3) Claude.ai web session [deferred]
STACK: SwiftUI + AppKit, Swift CLI binary for TUI, App Groups shared container, Keychain, TOML config, JSON cache

=== PHASE 1: PROJECT SCAFFOLDING ===
(A) Create Xcode project as macOS App with SwiftUI lifecycle targeting macOS 13+ +core
(A) Set LSUIElement=YES in Info.plist to suppress Dock icon and run as agent-only app +core
(A) Add App Groups entitlement with group identifier "group.dev.claudeusage" for shared container access between app and CLI binary +core
(A) Add Network Client and Keychain Access Groups entitlements under App Sandbox +core
(A) Create Swift Package Manager multi-target setup: main app target "ClaudeUsage", CLI target "claude-usage" +core
(A) Add swift-toml SPM dependency (https://github.com/LebJe/TOMLKit) for config parsing +core
(A) Configure App Groups shared container path constant: FileManager.containerURL(forSecurityApplicationGroupIdentifier:) stored as AppConstants.sharedContainerURL +core
(A) Add LaunchAtLogin SPM dependency (https://github.com/sindresorhus/LaunchAtLogin-Modern) for login item support +core

=== PHASE 2: CONFIG SYSTEM ===
(A) Define Config struct with Codable: accounts [Account], pollIntervalSeconds Int (default 300), tui TUIConfig, display DisplayConfig +config
(A) Define Account struct: id UUID, name String, type AccountType enum (claudeCode/anthropicAPI/claudeAI), isActive Bool, createdAt Date +config
(A) Define TUIConfig struct: layout [String] (ordered field names), colorScheme String, showLogo Bool, separatorChar String, labelWidth Int +config
(A) Define DisplayConfig struct: menubarStyle String ("icon"/"tokens"/"cost"), showBadge Bool, compactMode Bool, dualIcon Bool (default false) +config
(A) Define AccountType enum with rawValue String; claudeCode requires no credentials, anthropicAPI requires keychain key, claudeAI is .unsupported +config
(A) Implement ConfigManager class reading/writing ~/.config/claude-usage/config.toml using TOMLKit, creating directory if absent +config
(A) ConfigManager.load() returns Config, falls back to Config.default if file absent or parse fails +config
(A) ConfigManager.save(_ config: Config) serialises and writes atomically via FileManager replaceItem +config
(A) Config.default returns Config with one claudeCode account named "Local", pollIntervalSeconds 300, sane TUIConfig defaults +config
(A) Define SparklineConfig struct: enabled Bool (default true), windowHours Int (default 168), style String ("cost"/"tokens"), resolution Int (default 24 data points) +config
(A) Define ForecastConfig struct: enabled Bool (default true), showInPopover Bool (default true), showInTUI Bool (default false) +config
(B) Define WebhookConfig struct: enabled Bool (default false), url String, events [String] (e.g. ["threshold","daily_digest"]), payloadTemplate String? +config
(A) Define AnalyticsConfig struct: enabled Bool (default true), showMonthlyView Bool (default true), showHeatmap Bool (default false) +config
(B) Define ModelOptimizerConfig struct: enabled Bool (default true), cheapThresholdTokens Int (default 1000), showInPopover Bool (default true) +config
(A) Define iCloudSyncConfig struct: enabled Bool (default false), localOnly Bool (default true), containerIdentifier String (default "iCloud.dev.claudeusage") +config
(A) Define GlobalHotkeyConfig struct: enabled Bool (default true), modifiers [String] (default ["option","command"]), key String (default "c") +config
(C) Define AutomationRule struct in config layer: id UUID, name String, triggerType String ("cost_gt"/"tokens_gt"), threshold Double, shellCommand String, lastFiredAt Date?, enabled Bool; stored as [[automations]] array in TOML +config
(A) Extend Config struct to include: sparkline SparklineConfig, forecast ForecastConfig, webhook WebhookConfig, analytics AnalyticsConfig, modelOptimizer ModelOptimizerConfig, iCloudSync iCloudSyncConfig, hotkey GlobalHotkeyConfig, automations [AutomationRule] +config

=== PHASE 3: KEYCHAIN ===
(A) Implement KeychainManager enum with static func store(key: String, service: String, account: String) throws +keychain
(A) Implement KeychainManager.retrieve(service: String, account: String) throws -> String +keychain
(A) Implement KeychainManager.delete(service: String, account: String) throws +keychain
(A) Use service="claude-usage", account=account.id.uuidString for all keychain ops +keychain
(A) Wrap SecItem* calls in KeychainError enum: itemNotFound, duplicateItem, unexpectedData, underlying(OSStatus) +keychain

=== PHASE 4: DATA MODEL & CACHE ===
(A) Define UsageSnapshot struct: accountId UUID, timestamp Date, inputTokens Int, outputTokens Int, cacheCreationTokens Int, cacheReadTokens Int, totalCostUSD Double, modelBreakdown [ModelUsage] +cache
(A) Define ModelUsage struct: modelId String, inputTokens Int, outputTokens Int, costUSD Double +cache
(A) Define DailyAggregate struct: date DateComponents (year/month/day), snapshots [UsageSnapshot], totalInputTokens Int computed, totalOutputTokens Int computed, totalCostUSD Double computed +cache
(A) Implement CacheManager writing UsageSnapshot array to shared container at "usage_cache.json" via JSONEncoder +cache
(A) CacheManager.append(_ snapshot: UsageSnapshot) loads existing, appends, prunes entries older than 30 days, saves +cache
(A) CacheManager.latest(forAccount id: UUID) -> UsageSnapshot? returns most recent snapshot for account +cache
(A) CacheManager.history(forAccount id: UUID, days: Int) -> [UsageSnapshot] returns sorted ascending +cache
(A) CacheManager.todayAggregate(forAccount id: UUID) -> DailyAggregate computes today's totals +cache
(A) CacheManager uses file coordinator (NSFileCoordinator) for concurrent access safety between app and CLI +cache
(A) Define ForecastSnapshot struct: accountId UUID, generatedAt Date, projectedEODCostUSD Double, projectedEOWCostUSD Double, projectedEOMCostUSD Double, burnRatePerHour Double +cache
(A) Implement ForecastEngine.compute(history: [UsageSnapshot], now: Date) -> ForecastSnapshot: derive hourly burn rate from today's snapshots, extrapolate linearly to end-of-day/week/month; clamp projections at zero +cache
(B) Define ModelHint struct: accountId UUID, date Date, expensiveModelTokens Int, cheaperAlternativeExists Bool, estimatedSavingsUSD Double, recommendedModel String +cache
(B) Implement ModelOptimizerAnalyzer.analyze(breakdown: [ModelUsage], config: ModelOptimizerConfig) -> ModelHint?: if expensive-model token share > cheapThresholdTokens and cheaper alternative exists, compute savings delta using AnthropicAPIClient.pricingConstants +cache
(A) CacheManager.latestForecast(forAccount id: UUID) -> ForecastSnapshot? reads "forecast_cache.json" in shared container +cache
(A) CacheManager.saveForecast(_ forecast: ForecastSnapshot) writes/overwrites "forecast_cache.json" in shared container +cache

=== PHASE 5: CLAUDE CODE LOG PARSER (DATA SOURCE 1 — DEFAULT MVP) ===
(A) Implement ClaudeCodeLogParser targeting ~/.claude/ directory +logs
(A) ClaudeCodeLogParser.discoverSessionFiles() -> [URL] globs ~/.claude/projects/**/*.jsonl recursively +logs
(A) Define ClaudeCodeEntry Codable matching Claude Code JSONL schema: type String, message Message?, usage Usage? +logs
(A) Define Usage Codable: input_tokens Int, output_tokens Int, cache_creation_input_tokens Int?, cache_read_input_tokens Int? +logs
(A) ClaudeCodeLogParser.parseFile(_ url: URL) -> [ClaudeCodeEntry] reads line-by-line, skips malformed lines silently +logs
(A) ClaudeCodeLogParser.aggregateToday() -> UsageSnapshot sums all usage fields from today's entries across all session files, uses model "claude-code-local", zero cost (local logs lack pricing) +logs
(A) ClaudeCodeLogParser.aggregatePeriod(days: Int) -> [UsageSnapshot] groups by calendar day, returns one snapshot per day +logs
(A) Add FSEvents-based directory watcher on ~/.claude/projects/ to invalidate cache when new session files appear, post "ClaudeCodeLogsChanged" notification +logs
(A) ClaudeCodeLogParser handles file permission errors gracefully, emits to Logger(subsystem:) instead of crashing +logs

=== PHASE 6: ANTHROPIC API CLIENT (DATA SOURCE 2) ===
(B) Implement AnthropicAPIClient using URLSession async/await targeting https://api.anthropic.com +api
(B) AnthropicAPIClient.init(apiKey: String) stores key, sets default headers: x-api-key, anthropic-version: "2023-06-01", Content-Type: application/json +api
(B) Implement AnthropicAPIClient.fetchUsage(startDate: Date, endDate: Date) async throws -> AnthropicUsageResponse calling GET /v1/usage with query params start_date, end_date (yyyy-MM-dd) +api
(B) Define AnthropicUsageResponse Codable: data [AnthropicUsagePeriod], has_more Bool, first_id String?, last_id String? +api
(B) Define AnthropicUsagePeriod Codable: start_time String, end_time String, input_tokens Int, output_tokens Int, cache_creation_input_tokens Int, cache_read_input_tokens Int, model String +api
(B) AnthropicAPIClient.validateKey() async -> Bool calls GET /v1/models, returns true on 200, false on 401 +api
(B) Implement APIError enum: invalidKey, rateLimited(retryAfter: Int?), serverError(Int), decodingFailed, networkError(Error) +api
(B) Map HTTP 401 -> APIError.invalidKey, 429 -> APIError.rateLimited parsing Retry-After header, 5xx -> APIError.serverError +api
(B) AnthropicAPIClient converts AnthropicUsageResponse to [UsageSnapshot] applying pricing constants per model (claude-3-5-sonnet: $3/$15 per 1M in/out, claude-3-opus: $15/$75, claude-3-haiku: $0.25/$1.25, default: $0/$0) +api
(B) Pricing constants stored as static dict [String: (inputPer1M: Double, outputPer1M: Double)] on AnthropicAPIClient +api

=== PHASE 7: POLLING SERVICE ===
(A) Implement PollingService @MainActor class with timer: Timer? and accounts: [Account] +poll
(A) PollingService.start(config: Config) creates repeating Timer with config.pollIntervalSeconds interval, fires immediately on start +poll
(A) PollingService.stop() invalidates and nils timer +poll
(A) PollingService.pollOnce() async iterates active accounts, dispatches to correct parser/client by AccountType, appends result to CacheManager +poll
(A) PollingService.pollOnce() runs account fetches concurrently via async let or TaskGroup, max concurrency 3 +poll
(A) PollingService posts "UsageDidUpdate" via NotificationCenter after each pollOnce completion with userInfo containing updated accountIds +poll
(A) PollingService exposes @Published var lastPollDate: Date? and @Published var isPolling: Bool for UI binding +poll
(A) PollingService.forceRefresh() cancels existing poll, runs pollOnce immediately +poll
(A) PollingService reloads config from ConfigManager before each poll cycle to pick up live config changes +poll

=== PHASE 8: MENU BAR ===
(A) Implement MenuBarManager class creating NSStatusItem with variable length in NSStatusBar.system +menubar
(A) MenuBarManager sets status item button image to SF Symbol "chart.bar.fill" with renderingMode .template by default +menubar
(A) Implement MenuBarPopoverView SwiftUI view shown in NSPopover attached to status item button on click +menubar
(A) MenuBarPopoverView header row: app name, last-updated relative time (e.g. "2m ago"), spinner when isPolling=true +menubar
(A) MenuBarPopoverView account tabs or segmented control if multiple accounts configured, default to first active account +menubar
(A) MenuBarPopoverView per-account section: input tokens today, output tokens today, cache tokens today, estimated cost today +menubar
(A) MenuBarPopoverView 7-day trend bar chart using Swift Charts framework (macOS 13+) showing daily totalCostUSD +menubar
(A) MenuBarPopoverView footer: "Refresh Now" button calling PollingService.forceRefresh(), "Settings…" button opening settings window +menubar
(A) MenuBarManager updates status item button title with cost string (e.g. "$0.42") when DisplayConfig.menubarStyle == "cost" +menubar
(A) MenuBarManager updates status item button title with token count (e.g. "12k") when DisplayConfig.menubarStyle == "tokens" +menubar
(A) MenuBarManager observes "UsageDidUpdate" notification to refresh popover content without reopening popover +menubar
(A) Popover width 340pt, fixed; auto-dismissed on click outside via NSPopover.behavior = .transient +menubar

=== PHASE 9: SETTINGS WINDOW ===
(A) Implement SettingsWindowController as NSWindowController opening a 600x480 SwiftUI Settings view +settings
(A) SettingsView uses TabView with tabs: Accounts, Display, Polling, Analytics, Integrations, Automations, Sync, CLI, About +settings
(A) AccountsTab: List of Account rows showing name, type badge, active toggle; add/delete buttons +settings
(A) AccountsTab "Add Account" sheet: name TextField, AccountType Picker, conditional API key SecureField for anthropicAPI type, Save validates before dismissing +settings
(A) AccountsTab save action: stores Account in config, stores API key in Keychain, saves config via ConfigManager +settings
(A) AccountsTab delete: removes from config, removes from Keychain, calls ConfigManager.save +settings
(A) AccountsTab validates anthropicAPI key on add via AnthropicAPIClient.validateKey(), shows inline error on failure +settings
(A) DisplayTab: Picker for menubarStyle ("Icon only"/"Show cost"/"Show tokens"), Toggle showBadge, Toggle compactMode +settings
(A) PollingTab: Slider or TextField for pollIntervalSeconds (range 60-3600), preview label "Every X minutes" +settings
(A) CLITab: instructions to install claude-usage binary, button "Install to /usr/local/bin" running privileged copy with SMJobBless or user-confirmed shell command +settings
(A) CLITab: .zshrc snippet TextField pre-filled with "claude-usage" showing copy-to-clipboard button +settings
(A) CLITab: TUI layout editor: reorderable List of field name strings matching TUIConfig.layout; drag-to-reorder via .onMove +settings
(A) AboutTab: app version from Bundle, GitHub link, data source attribution +settings
(A) AnalyticsTab: Toggle for ForecastConfig.enabled ("Show spend forecasts"), Toggle ForecastConfig.showInPopover, Toggle ForecastConfig.showInTUI, Toggle AnalyticsConfig.showMonthlyView ("Show monthly view in History"), Toggle AnalyticsConfig.showHeatmap ("Show hourly usage heatmap"), Toggle ModelOptimizerConfig.enabled ("Show model cost hints"), Stepper ModelOptimizerConfig.cheapThresholdTokens, Toggle ModelOptimizerConfig.showInPopover; all changes persist via ConfigManager.save +settings
(B) IntegrationsTab: Toggle WebhookConfig.enabled, TextField WebhookConfig.url (validated as URL on input), MultiSelect checkboxes for WebhookConfig.events (["threshold","daily_digest","weekly_summary"]), TextEditor WebhookConfig.payloadTemplate (optional JSON template with {{variables}}), Button "Send Test Payload" calling WebhookService.sendTest() showing inline success/error badge +settings
(A) HotkeyTab (embedded in Display or as subtab): Toggle GlobalHotkeyConfig.enabled, key recorder view (custom NSView wrapping Carbon event tap) showing current binding e.g. "⌥⌘C", button "Reset to Default" restoring ["option","command"]+"c" +settings
(C) AutomationsTab: List of AutomationRule rows showing name, trigger summary, enabled toggle; Add button opens sheet: name TextField, triggerType Picker (Cost >  / Tokens >), threshold TextField, shellCommand TextField, test "Run Now" button with stdout/stderr output view; delete swipe action removes rule from config +settings
(A) SyncTab: Toggle iCloudSyncConfig.enabled with inline note "Requires iCloud Drive", when enabled shows last sync timestamp and sync status badge (idle/syncing/error), Button "Sync Now" calling iCloudSyncManager.syncNow(), Toggle iCloudSyncConfig.localOnly; disabled state shows "iCloud sync is off — data stays local only" +settings
(A) Settings window opens via NSApp.activate(ignoringOtherApps: true) + window.makeKeyAndOrderFront, reverts activation policy to .accessory on window close +settings

=== PHASE 10: TUI BINARY (claude-usage CLI) ===
(A) Create Swift executable target "claude-usage" in same Xcode project, entry point main.swift +tui
(A) CLI binary reads shared container usage_cache.json via AppConstants.sharedContainerURL (same App Group) +tui
(A) CLI binary reads ~/.config/claude-usage/config.toml for layout, colors, account filter +tui
(A) Implement ANSIColor enum: black, red, green, yellow, blue, magenta, cyan, white, bright variants; escape(code:) -> String returning "\u{1B}[\(code)m" +tui
(A) Implement TUIRenderer struct that takes [UsageSnapshot] + TUIConfig and renders to String +tui
(A) TUIRenderer renders Claude ASCII logo (8-line block, left column, 20 chars wide) alongside data (right column) in side-by-side layout matching fastfetch style +tui
(A) TUIRenderer field mapping: "input_tokens" -> "Input Tokens", "output_tokens" -> "Output Tokens", "cache_tokens" -> "Cache Tokens", "cost_usd" -> "Cost Today", "last_updated" -> "Updated", "model_breakdown" -> per-model sub-rows +tui
(A) TUIRenderer pads label to TUIConfig.labelWidth (default 18), right-aligns value, applies per-field ANSI color from colorScheme +tui
(A) TUIRenderer colorScheme "default": labels cyan, values white, header bright white, separator dark gray +tui
(A) TUIRenderer prints separator line (TUIConfig.separatorChar repeated 40 times) between accounts +tui
(A) CLI parses args: --account NAME (filter), --no-color (strip ANSI), --format json (JSON output), --help +tui
(A) --format json outputs raw UsageSnapshot as JSON to stdout for piping to other tools +tui
(A) CLI exits 0 on success, 1 if cache file absent (prints "Run ClaudeUsage.app first to populate data"), 2 on parse error +tui
(A) CLI binary compiled with -O whole-module-optimization, target launch time <100ms +tui

=== PHASE 11: NOTIFICATIONS ===
(B) Implement NotificationManager using UNUserNotificationCenter +notify
(B) NotificationManager.requestPermission() called once on first launch, stores result in UserDefaults +notify
(B) NotificationManager.checkThreshold(snapshot: UsageSnapshot, limitUSD: Double) posts UNNotificationRequest if snapshot.totalCostUSD > limitUSD +notify
(B) Notification content: title "Claude Usage Alert", body "Account '\(name)' reached $\(cost) today (limit: $\(limit))" +notify
(B) CostLimit configurable per-account in AccountsTab Settings as optional Double field, default nil (disabled) +notify
(B) Throttle threshold notifications: store last-notified date per account in UserDefaults, skip if already notified today +notify

=== PHASE 12: PERFORMANCE & POLISH ===
(A) All CacheManager file I/O on background DispatchQueue.global(qos: .utility), results dispatched to MainActor via Task { @MainActor in } +core
(A) PollingService.pollOnce() wrapped in Task with .background priority, never blocks main thread +core
(A) MenuBarPopoverView uses LazyVStack for account list to avoid rendering off-screen rows +menubar
(A) App launches in agent mode with no visible window; first render of popover deferred until user clicks status item +core
(A) ClaudeCodeLogParser caches file modification dates; skips re-parsing unchanged files on subsequent poll cycles +logs
(A) UsageSnapshot encoding uses ISO8601DateEncodingStrategy for cross-binary compatibility +cache
(A) Stale data indicator: if CacheManager.latest timestamp > pollIntervalSeconds * 2 old, MenuBar shows orange dot badge +menubar
(A) CLI binary handles missing shared container gracefully (e.g. app not yet run) with clear exit message +tui

=== PHASE 13: DUAL ICON STUB (LOW PRIORITY) ===
(C) When DisplayConfig.dualIcon == true, create second NSStatusItem for second active account if exactly 2 accounts configured +menubar
(C) Second NSStatusItem shows second account's cost/token in menu bar, opens same popover scrolled to that account +menubar
(C) If dualIcon == true but fewer than 2 active accounts, ignore silently without error +menubar

=== PHASE 14: CLAUDE.AI WEB SESSION STUB (DEFERRED) ===
(C) Add AccountType.claudeAI case to enum with isSupported computed property returning false +api
(C) In AccountsTab, show "(coming soon)" label next to Claude.ai account type option, disable Save if type selected +settings
(C) Stub ClaudeAIWebClient struct with fetchUsage() async throws that always throws APIError.unsupported +api

=== PHASE 15: MENUBAR SPARKLINE ===
(A) Implement SparklineRenderer struct: takes [Double] (cost or token values), bounds CGRect, returns CGPath via Core Graphics +menubar
(A) SparklineRenderer.render(values: [Double], in rect: CGRect) -> CGPath: normalise values to [0,1], draw polyline; fill area under curve with 20% alpha stroke color +menubar
(A) Implement MenuBarSparklineImage: creates NSImage(size: NSSize(width:22, height:16)), locks focus, draws SparklineRenderer path in white (.template rendering), unlocks +menubar
(A) MenuBarManager.updateSparklineIcon(snapshots: [UsageSnapshot], config: SparklineConfig) samples last config.windowHours of snapshots at config.resolution evenly-spaced points, feeds to SparklineRenderer, sets result as statusItem button image +menubar
(A) SparklineConfig.style "cost" samples totalCostUSD; "tokens" samples (inputTokens+outputTokens) +menubar
(A) SparklineConfig.enabled == false falls back to static SF Symbol "chart.bar.fill" icon (existing behavior) +menubar
(A) MenuBarManager regenerates sparkline image on each "UsageDidUpdate" notification; throttles to max 1 redraw per 5 seconds using DispatchWorkItem cancellation +menubar
(A) Add SparklineTab or subsection in DisplayTab: Toggle enabled, Picker style ("Cost"/"Tokens"), Stepper windowHours (range 24–720 in steps of 24), Stepper resolution (range 12–48) +settings

=== PHASE 16: SPEND FORECASTING ===
(A) PollingService.pollOnce() calls ForecastEngine.compute after each successful aggregate, stores result via CacheManager.saveForecast +poll
(A) MenuBarPopoverView forecast section (shown when ForecastConfig.showInPopover == true): three rows "EOD", "EOW", "EOM" with projected cost values formatted as "$X.XX", shown below today's stats +menubar
(A) Forecast section header "Projected Spend" with SF Symbol "chart.line.uptrend.xyaxis"; hidden entirely if ForecastConfig.enabled == false +menubar
(A) ForecastEngine returns nil if fewer than 2 UsageSnapshots exist for today (insufficient data); popover shows "Not enough data yet" placeholder in forecast section +cache
(A) TUIRenderer adds forecast block when ForecastConfig.showInTUI == true: three rows EOD/EOW/EOM after standard fields, separated by half-separator +tui
(A) CLI --forecast flag forces forecast block output regardless of TUIConfig setting +tui

=== PHASE 17: WEBHOOK / SLACK ALERTS ===
(B) Implement WebhookService class with URLSession async/await +integrations
(B) WebhookService.send(event: WebhookEvent, snapshot: UsageSnapshot, config: WebhookConfig) async throws: serialises payload, POST to config.url with Content-Type application/json, throws on non-2xx +integrations
(B) Define WebhookEvent enum: thresholdBreached(limitUSD: Double), dailyDigest, weeklyDigest +integrations
(B) WebhookService.buildPayload(event: WebhookEvent, snapshot: UsageSnapshot, template: String?) -> Data: if template non-nil, replace {{account}}, {{cost}}, {{tokens}}, {{date}}, {{event}} tokens; else use default JSON schema +integrations
(B) Default payload schema: {"event": String, "account": String, "timestamp": ISO8601, "cost_usd": Double, "input_tokens": Int, "output_tokens": Int} +integrations
(B) NotificationManager.checkThreshold posts webhook via WebhookService if WebhookConfig.enabled && "threshold" in WebhookConfig.events, in addition to local UNNotification +notify
(B) Implement PollingService daily digest: track lastDailyDigestDate in UserDefaults; if current poll crosses midnight and "daily_digest" in events, call WebhookService.send(.dailyDigest) with yesterday's DailyAggregate +poll
(B) Implement weekly digest: fires on Monday 00:00 crossing if "weekly_summary" in events +poll
(B) WebhookService.sendTest(config: WebhookConfig) async -> Result<Void, Error>: sends dummy payload with event "test", returns result for Settings UI display +integrations
(B) WebhookConfig.url validated via URL(string:) != nil check; invalid URL disables send and shows error badge in IntegrationsTab +integrations

=== PHASE 18: GLOBAL HOTKEY ===
(A) Implement HotkeyManager class using Carbon RegisterEventHotKey API; registered on app launch if GlobalHotkeyConfig.enabled == true +hotkey
(A) HotkeyManager.register(config: GlobalHotkeyConfig) maps config.modifiers ["option","command"] to cmdKey|optionKey Carbon modifiers, config.key "c" to keycode via UCKeyTranslate lookup table +hotkey
(A) HotkeyManager hotkey handler calls MenuBarManager.togglePopover() on main thread via DispatchQueue.main.async +hotkey
(A) HotkeyManager.unregister() calls UnregisterEventHotKey; called on config change or app termination +hotkey
(A) HotkeyManager observes ConfigManager changes; if hotkey config differs, unregisters old and registers new binding +hotkey
(A) Implement KeyRecorderView: NSViewRepresentable wrapping custom NSView that captures keyDown + modifier flags, converts to GlobalHotkeyConfig, calls onChange callback; shows current binding as e.g. "⌥⌘C" using NSEvent.ModifierFlags description +settings
(A) HotkeyTab saves new binding via ConfigManager.save, re-registers via HotkeyManager.register; shows "Hotkey registered" confirmation badge for 2 seconds +settings

=== PHASE 19: MONTHLY / ROLLING ANALYTICS ===
(A) Implement AnalyticsEngine struct with static methods operating on [UsageSnapshot] +analytics
(A) AnalyticsEngine.monthToDate(snapshots: [UsageSnapshot], account: UUID) -> DailyAggregate: filter current calendar month, return aggregate +analytics
(A) AnalyticsEngine.rollingAverage(snapshots: [UsageSnapshot], days: Int, account: UUID) -> Double: mean of daily totalCostUSD over last `days` days +analytics
(A) AnalyticsEngine.heatmap(snapshots: [UsageSnapshot], account: UUID) -> [[Double]]: 7x24 grid [weekday][hour] of average cost; normalised to [0,1] +analytics
(A) Implement HistoryView SwiftUI view shown as a separate popover section or Sheet triggered by "History" button in MenuBarPopoverView footer +menubar
(A) HistoryView tab bar: "7 Day" (existing chart), "30 Day" bar chart, "Month-to-Date" totals, "Heatmap" +analytics
(A) HistoryView "30 Day" tab: Swift Charts BarMark for each of last 30 days, x-axis date labels every 7 days, y-axis cost in USD; respects AnalyticsConfig.showMonthlyView toggle +analytics
(A) HistoryView "Month-to-Date" tab: total cost, total input/output/cache tokens, daily average, highest single-day cost with date label +analytics
(A) HistoryView "Heatmap" tab: 7×24 grid of colored Rectangle marks (cool→hot color scale), x-axis hours 0–23, y-axis Mon–Sun; shown only if AnalyticsConfig.showHeatmap == true, else placeholder "Enable heatmap in Analytics settings" +analytics
(A) HistoryView hidden entirely if AnalyticsConfig.enabled == false; History button in popover footer also hidden +menubar
(A) CLI --history flag prints 30-day summary table (date | cost | input | output) to stdout; --heatmap flag prints 7×24 ASCII grid using block chars ░▒▓█ +tui

=== PHASE 20: MODEL COST OPTIMIZER HINTS ===
(B) PollingService.pollOnce() calls ModelOptimizerAnalyzer.analyze after each aggregate if ModelOptimizerConfig.enabled == true, stores ModelHint to "model_hints.json" in shared container via CacheManager +poll
(B) ModelOptimizerAnalyzer.analyze: if any model in breakdown contains "opus" or "sonnet" and session average output_tokens < cheapThresholdTokens, flag as cheaperAlternativeExists; compute estimatedSavingsUSD as (currentModel.costUSD - haikuCost) +cache
(B) MenuBarPopoverView model hint banner: shown below forecast section when ModelHint.cheaperAlternativeExists == true and ModelOptimizerConfig.showInPopover == true; text "↓ ~$X.XX/day switching to {{recommendedModel}}" with SF Symbol "lightbulb" in yellow +menubar
(B) Model hint banner has dismiss button storing dismissedDate per account in UserDefaults; re-surfaces after 7 days +menubar
(B) TUIRenderer adds model hint row when hint present and TUIConfig includes "model_hint" field name in layout +tui
(B) AnalyticsTab ModelOptimizerConfig.cheapThresholdTokens Stepper range 100–10000, label "Flag sessions shorter than N output tokens" with explanatory footnote +settings

=== PHASE 21: ICLOUD / MULTI-MACHINE AGGREGATION (CORE, LOCAL-FIRST) ===
(A) Implement iCloudSyncManager class; when iCloudSyncConfig.localOnly == true all methods are no-ops, data remains in local App Groups container only +sync
(A) iCloudSyncManager.containerURL: FileManager.url(forUbiquityContainerIdentifier: config.containerIdentifier)?.appendingPathComponent("usage_cache.json") +sync
(A) iCloudSyncManager.syncNow() async: reads local usage_cache.json, reads remote if exists, merges by deduplicating on (accountId, timestamp) keeping union, writes merged result to both local and iCloud container using NSFileCoordinator +sync
(A) Merge conflict resolution: prefer entry with higher (inputTokens + outputTokens) when timestamps match within 1 second tolerance +sync
(A) iCloudSyncManager.startMetadataQuery() sets up NSMetadataQuery monitoring iCloud container for changes to usage_cache.json; on change notification calls syncNow() +sync
(A) PollingService.pollOnce() calls iCloudSyncManager.syncNow() at end of cycle if iCloudSyncConfig.enabled == true +poll
(A) SyncTab displays sync status via @Published var syncState: SyncState enum (idle, syncing, error(String), disabled); last sync timestamp from UserDefaults("lastCloudSyncDate") +settings
(A) iCloudSyncManager posts "iCloudSyncDidComplete" notification; MenuBarManager observes to refresh popover (merged remote data may change displayed totals) +sync
(A) iCloudSyncConfig.enabled == false (default) means zero iCloud entitlement usage; iCloud container entitlement added to App Sandbox only when feature is shipped and enabled; document in CLITab/AboutTab that iCloud is opt-in +sync
(A) CacheManager.history merges local + remote snapshots transparently when iCloudSyncConfig.enabled, so all consumers (popover, TUI, analytics) see aggregated multi-machine data without changes +cache

=== PHASE 22: TRIGGER-BASED AUTOMATIONS (STRETCH GOAL) ===
(C) Implement AutomationEngine struct; evaluate(rules: [AutomationRule], snapshot: UsageSnapshot) -> [AutomationRule] returns rules whose trigger condition is met +automations
(C) AutomationEngine.fire(rule: AutomationRule) async: runs rule.shellCommand via Process(), captures stdout/stderr, logs result to Logger(subsystem:); updates rule.lastFiredAt in config via ConfigManager.save +automations
(C) PollingService.pollOnce() calls AutomationEngine.evaluate then fires matching enabled rules concurrently via TaskGroup; skips rules fired within last poll interval to prevent re-fire on same data +poll
(C) AutomationRule triggerType "cost_gt": fires if snapshot.totalCostUSD > threshold +automations
(C) AutomationRule triggerType "tokens_gt": fires if (inputTokens + outputTokens) > threshold +automations
(C) AutomationsTab "Run Now" test button: calls AutomationEngine.fire(rule:) on selected rule, shows stdout/stderr in inline expandable text view, does NOT update lastFiredAt (test mode) +settings
(C) AutomationRule shellCommand executed with $CLAUDE_COST, $CLAUDE_TOKENS, $CLAUDE_ACCOUNT env vars set to snapshot values for use in user scripts +automations
(C) AutomationEngine sanitises shellCommand: rejects empty string, warns user in Settings if command contains rm -rf or equivalent destructive patterns via static regex check +automations

=== PHASE 23: APPLESCRIPT / SHORTCUTS BRIDGE (STRETCH GOAL) ===
(C) Add NSAppleScriptEnabled=YES and scripting definition file (ClaudeUsage.sdef) to Info.plist +scriptable
(C) Define scriptable suite "Claude Usage Suite" with classes: Application (std), UsageRecord (properties: accountName String, inputTokens Int, outputTokens Int, costUSD Double, lastUpdated Date) +scriptable
(C) Implement get today usage AppleScript command returning list of UsageRecord for all active accounts from CacheManager +scriptable
(C) Implement get forecast AppleScript command returning ForecastSnapshot fields as record (projectedEOD, projectedEOW, projectedEOM) +scriptable
(C) Implement refresh AppleScript command calling PollingService.forceRefresh() and awaiting completion before returning +scriptable
(C) Expose same surface via Intent framework: define AppIntent structs GetTodayUsageIntent and GetForecastIntent conforming to AppIntent; add Intents extension target linked to shared App Group container so Shortcuts.app can read data without launching main app +scriptable
(C) GetTodayUsageIntent returns IntentResult with [UsageRecord] serialised; visible in Shortcuts.app as "Get Claude Usage Today" action +scriptable
(C) GetForecastIntent returns IntentResult with projected costs; visible as "Get Claude Spend Forecast" action in Shortcuts.app +scriptable
